name: ci

on: push

jobs:
  ci:
    runs-on: ubuntu-latest
    steps: 
      - name: checkout repo
        uses: actions/checkcout@v3
      - name: Install dependencies 
        run: npm install
      - name: Run test
        run: npm test
        
